To run xv6, use the command "make qemu".

Use the command ps by itself or with one of the following flags: 

-st    sort process table by state
-st    sort process table by running-time
-sz    sort process table by process size
-n     print number of processes in the process table
-s <query>     search a process by name
-i <id>        search a process by id
-ir <id> <id>  displays processes with ids within range

